{
  "name": "mongodb-migrate-between-clusters",
  "version": "1.0.0",
  "description": "This project can be used to migrate all collections and documents from one MongoDB cluster to another MongoDB cluster",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "migrate": "ts-node --esm --experimental-specifier-resolution node index.ts"
  },
  "author": "Thom Trentelman",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.1",
    "mongoose": "^6.5.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
